export interface CompareFunction<T> {
    (a: T, b: T): number;
}
/**
 * Collects all values from an async iterator, sorts them
 * using the passed function and yields them
 */
export default function sort<T>(source: AsyncIterable<T> | Iterable<T>, sorter: CompareFunction<T>): AsyncGenerator<T, void, undefined>;
//# sourceMappingURL=index.d.ts.map