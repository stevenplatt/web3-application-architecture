import type { ComponentMetricsTracker } from '@libp2p/interface-metrics';
export interface TrackedMapInit {
    metrics: ComponentMetricsTracker;
    system?: string;
    component: string;
    metric: string;
}
export interface CreateTrackedMapOptions {
    metrics?: ComponentMetricsTracker;
    system?: string;
    component: string;
    metric: string;
}
export declare function trackedMap<K, V>(config: CreateTrackedMapOptions): Map<K, V>;
//# sourceMappingURL=index.d.ts.map